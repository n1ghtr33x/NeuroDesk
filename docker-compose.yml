services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: neurodesk
      POSTGRES_USER: root
      POSTGRES_PASSWORD: neurodesk
    ports:
      - "5432:5432"
    volumes:
      - neurodesk_pg:/var/lib/postgresql/data

volumes:
  neurodesk_pg: